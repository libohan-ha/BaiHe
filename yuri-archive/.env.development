# 开发环境配置
# API 请求会通过 Vite 的 proxy 代理到 http://localhost:3000

# API Base URL（可选，默认为空字符串，使用 Vite proxy）
# VITE_API_BASE_URL=

# 后端服务器地址（用于图片等静态资源，默认 http://localhost:3000）
# VITE_BACKEND_URL=http://localhost:3000

